# 점검 기록

## 현재 상태

소비자 웹 화면을 고친 뒤, 아래 명령을 실행했습니다. 커밋·배포는 하지 않았습니다.

## 문제점과 고친 것

| 항목 | 이전 | 이후 |
| --- | --- | --- |
| 가짜 아이디 | `putduk4821` 표시 | 준비 중 안내 |
| 가짜 비밀번호 저장 | 저장했다고 표시 | 준비 중 안내 |
| 가짜 문의 접수 | 남겼다고 표시 | 준비 중 안내 |
| 가짜 공지·이벤트 | 고정 글 목록 | 아직 없다고 안내 |
| 가짜 AI 답변 | 로컬에서 만든 답을 실제처럼 표시 | 연결 준비 중 + 오류 안내 |
| 가짜 0원 | 값 없어도 0으로 표시 | 값이 있을 때만 숫자 |
| 본인확인 | 보내자마자 완료 | 요청 보냄 / 확인 완료 구분 |
| 없는 화면 | 안내 없음 | 없는 화면 안내 |
| 퍼뜩AI 보호 | 로그인 없이 입장 | 로그인 필요 |
| 어려운 알림 | 실패, 토큰, 상태 숫자 | 쉬운 한국어 + 이모티콘 |
| 하단 탭 | 설명 부족, 나 하위가 홈으로 보임 | 설명 추가, 나/지갑은 나 탭 |

## 실행한 명령

작업 후 아래를 실행합니다.

- `pnpm install --frozen-lockfile`
- `pnpm lint`
- `pnpm build`
- `pnpm exec tsc --noEmit`

결과는 이 문서 아래 “명령 결과”에 적습니다.

## 브라우저에서 못 한 것

이 환경에는 화면을 직접 누르는 도구가 없었습니다. 그래서 데스크톱·모바일 실기기 클릭, 새로고침, 뒤로가기는 명령 검사로만 확인했습니다. 실제 휴대폰에서 한 번 더 확인이 필요합니다.

## 남은 문제

- 아이디 찾기, 비밀번호 재설정, 문의 접수, 공지, 이벤트, 퍼뜩AI 답변은 백엔드가 필요합니다.
- 로그인하지 않은 홈은 소개 화면입니다. 하단 탭은 로그인 후에 보입니다.
- 거래 진행 연출은 실제 진행 값이 없어 열지 않습니다.

## 하지 않은 일

- commit / push / merge / deploy 하지 않았습니다.
- 백엔드와 관리자 화면을 수정하지 않았습니다.

## 명령 결과

| 명령 | 결과 |
| --- | --- |
| `pnpm install --frozen-lockfile` | 통과 (exit 0) |
| `pnpm lint` | 통과 (exit 0). 오류 0. `<img>` 관련 기존 경고만 있음 |
| `pnpm exec tsc --noEmit` | 통과 (exit 0) |
| `pnpm build` | 통과 (exit 0). 화면 31개 생성. 지정 경로 모두 포함 |

브라우저로 직접 누르며 확인하는 도구는 이 환경에 없어, 화면 클릭 점검은 하지 못했습니다.
