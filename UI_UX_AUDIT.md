# 퍼뜩 UI/UX 점검

현재 `codex/uiux-polish-no-fomo-20260909` 브랜치의 소비자 웹 UI 기준 점검입니다. 기존 데스크 시각 언어, 카드, 그리드, 브레이크포인트와 앰버서더 사진은 유지합니다.

## 이번 슬라이스에서 반영한 것

- 데스크톱과 모바일의 기본 탭을 같은 데이터에서 읽도록 정리했습니다.
- 탭은 홈 / 기회 / 퍼뜩AI / 초대 / 나로 표시합니다.
- `/work`는 기회 목록으로 유지하고, 완료한 업무와 정산은 `/me/records`로 분리했습니다.
- “나” 화면의 기록 입구도 `/me/records`로 연결했습니다.
- 참여 버튼 문구를 “이 기회로 수익 벌기”로 통일했습니다.
- 서버가 준 정산 상태와 실제 금액만 실행 결과에 반영합니다. 결과를 사용자가 고르거나 금액을 만들어 보여 주지 않습니다.
- 큰 금액은 원화를 먼저 표시하고, 원화 값이 없으면 임의 환율로 채우지 않습니다.
- 지갑·입출금 내역에서 빈 금액을 가짜 0원이나 대시로 채우지 않습니다.
- 퍼뜩AI는 자유 입력, 이전 대화, 타이핑 표시, 서버 응답과 근거 이동을 유지합니다.
- 인증 화면에서 출시 사용자용 확인 위젯 흐름을 새로 요구하지 않도록 정리했습니다.
- 공개 정적 파일 캐시 규칙을 보완해 큰 화면 자산의 재방문 비용을 줄였습니다.

## 보류한 것

- 초기 출시 FOMO, 참여자 수, 실시간 움직임과 같은 연출은 넣지 않았습니다.
- 멤버십 카드·등급 SVG·등급 기준 개편은 이번 UI 연결 슬라이스의 범위가 아닙니다.
- 체험 운용자본과 정산 엔진은 프론트에서 만들지 않습니다.

## 배포 전 확인

로컬에서 아래 명령을 실행하고, 실제 로그인 세션으로 데스크톱·모바일을 확인해야 합니다.

```bash
pnpm install --frozen-lockfile
pnpm lint
pnpm exec tsc --noEmit
pnpm build
pnpm preview
```

확인 항목은 탭 주소·라벨, 새로고침과 브라우저 뒤로가기, API 실패 빈 상태, 원화/USDT 우선순위, 출금 step-up, 본인확인, 자유 입력 AI, 가로 스크롤 여부입니다.
